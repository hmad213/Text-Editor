{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Qt App",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/QtApp.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build",
            "MIMode": "gdb"
        }
    ]
}